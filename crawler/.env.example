# 爬虫微服务（Steam / Epic）配置模板
#
# 用法：把本文件复制为同目录下的 .env，按需修改。.env 已被 .gitignore 排除。

# 访问 Steam / Epic 时走的 HTTP 代理，留空表示直连。
# 国内网络通常需要挂一个本地代理，例如 Clash 默认的 http://127.0.0.1:7890
CRAWLER_PROXY=

# 单独运行 `python app.py` 时的监听端口。
# 由 Java 启动时（PythonManager）会覆盖这个值，两侧共用 app.properties 里的 crawler.port。
CRAWLER_PORT=8099